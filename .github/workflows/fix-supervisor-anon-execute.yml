name: Fix supervisor RPC anon privilege

on:
  push:
    branches:
      - agent/hybrid-receipt-build-2-v1

permissions:
  contents: write

jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/hybrid-receipt-build-2-v1
          fetch-depth: 0
      - name: Tighten execute privilege and remove workflow
        run: |
          python - <<'PY'
          from pathlib import Path
          p = Path('supabase/migrations/20260801143000_hybrid_physical_receipt_supervisor_initial_decision_rpc_v1.sql')
          text = p.read_text()
          old = """REVOKE ALL
ON FUNCTION public.staff_decide_physical_receipt_review_v1(uuid,text,jsonb,text,text)
FROM PUBLIC;
"""
          new = """REVOKE ALL
ON FUNCTION public.staff_decide_physical_receipt_review