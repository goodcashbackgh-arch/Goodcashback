# Deployment recovery note — 2026-05-07

Main was restored to the last known READY code state after a failed manual edit to `RefundEvidenceModeSelector.tsx`.

This docs-only commit exists to trigger a fresh Vercel deployment using the restored code.
